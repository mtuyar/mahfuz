[build]
  base = "apps/web"
  command = "pnpm build && cp -r .netlify dist/ && sed -i 's|../../../dist/server|../../../server|' dist/.netlify/v1/functions/server.mjs"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
